# Control Plane Snapshots

Generated read-model snapshots live here for local dashboard bootstrap. They
are runtime artifacts and must not be committed; production runtime state is
stored by the configured filesystem or PostgreSQL adapter.
