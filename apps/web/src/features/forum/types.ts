import type {
  ArticleCategoryKey,
  ArticleDetail,
  ArticleListResponse,
  ArticleSummary,
} from '@harness/contracts'

export type ForumCategoryKey = 'all' | ArticleCategoryKey

export type { ArticleDetail, ArticleListResponse, ArticleSummary }
